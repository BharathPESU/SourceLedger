"""Pydantic models — the source-of-truth data contracts for SourceLedger.

This package is built first (contract-first development) so that every
agent, API route, and database table works against a shared, validated
schema rather than ad-hoc dicts.
"""
